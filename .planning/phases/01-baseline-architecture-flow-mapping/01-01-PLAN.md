---
phase: 01-baseline-architecture-flow-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-baseline-architecture-flow-mapping/baseline-architecture.dot
  - gui-dashboard-backend-feature-langfuse/package.json
  - gui-dashboard-frontend-main/package.json
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-03
user_setup: []
must_haves:
  truths:
    - "Automated dependency graph exists for backend and frontend"
    - "Knip report identifies unused dependencies"
    - "Architecture documented as derived from code, not manual entry"
  artifacts:
    - path: ".planning/phases/01-baseline-architecture-flow-mapping/baseline-architecture.dot"
      provides: "Automated dependency graph"
    - path: ".planning/phases/01-baseline-architecture-flow-mapping/knip-report.json"
      provides: "Unused code identification"
  key_links:
    - from: "dependency-cruiser"
      to: "ARCHITECTURE.md"
      via: "automated export"
---

<objective>
Establish baseline architecture truth using automated static analysis (Knip/dependency-cruiser).

Purpose: Fulfill AUDIT-01 and AUDIT-03 by validating the system architecture via automated code analysis tools, ensuring our documentation is derived from reality.
Output: Dependency graph and unused code report.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@gui-dashboard-backend-feature-langfuse/package.json
@gui-dashboard-frontend-main/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install audit tools</name>
  <files>gui-dashboard-backend-feature-langfuse/package.json, gui-dashboard-frontend-main/package.json</files>
  <action>
    Add `knip` and `dependency-cruiser` to devDependencies in both `package.json` files. Run `npm install` in both directories to ensure tools are ready for audit.
  </action>
  <verify>
    <automated>cd gui-dashboard-backend-feature-langfuse && npx knip --version && cd ../gui-dashboard-frontend-main && npx knip --version</automated>
  </verify>
  <done>Audit tools installed and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Generate architecture baseline</name>
  <files>.planning/phases/01-baseline-architecture-flow-mapping/baseline-architecture.dot, .planning/phases/01-baseline-architecture-flow-mapping/knip-report.json</files>
  <action>
    Run `dependency-cruiser` on both project directories to generate architecture graphs in DOT format. Run `knip` in both directories to identify unused exports. Save outputs to the phase directory.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-baseline-architecture-flow-mapping/baseline-architecture.dot && test -f .planning/phases/01-baseline-architecture-flow-mapping/knip-report.json</automated>
  </verify>
  <done>Baseline truth files generated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool execution | Static analysis tools read internal code structures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Code | Accept | Analysis tools stay within local machine environment |
</threat_model>

<verification>
Automated dependency graph exists for backend and frontend. Knip report identifies unused dependencies. Architecture documentation now relies on these files instead of manual entry.
</verification>

<success_criteria>
Dependency graph generated, unused code flagged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-architecture-flow-mapping/01-01-SUMMARY.md`
</output>
